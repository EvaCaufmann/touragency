@startuml
start

partition Авторизация и выбор тура {
:Клиент заходит на сайт/в приложение;
repeat
backward :Система предлагает вход/регистрацию;
repeat while (Клиент авторизован в системе?) is (Нет) not (Да)
:Клиент выбирает тур из каталога;
:Клиент указывает дату, количество человек и доп. услуги;
:Система отображает предварительный счет;
:Клиент подтверждает заявку;
}

partition Обработка заявки {
:Система сохраняет заявку и уведомляет менеджера;
:Менеджер получает уведомление о новой заявке;
:Менеджер проверяет доступность тура; #Повтор;
}

if (Тур доступен?) then (Да)
partition Подтверждение бронирования системой{
    :Менеджер подтверждает бронирование;
    :Система уведомляет клиента о подтверждении;
    :Менеджер оформляет заказ у туроператора;
    :Менеджер уведомляет клиента о подтверждении;
}
    
    partition Выбор услуг{
        :Система отображает доступные варианты;
        :Клиент выбирает услуги (билеты/отель);
        :Система подтверждает выбор;
    }
        partition Оплата {
        :Система перенаправляет клиента на оплату;
    note right
        Не более 3 попыток  
    end note
        if (Оплата успешна?) then (Да)
            :Система формирует электронный билет/ваучер;
            :Система отправляет билет клиенту;
        else (Нет)
            :Система уведомляет о неудачной оплате;
            :Система уведомляет клиента об отказе;
            end
        endif
        }
   
else (Нет)
partition Поиск альтернативного тура{
    :Менеджер предлагает клиенту альтернативные туры;
    if (Клиент принял альтернативу?) then (Да)
        --> Повтор
    else (Нет)
        :Менеджер отклоняет заявку;
        :Система уведомляет клиента об отказе;
        end
    endif
}
endif

stop

@enduml

